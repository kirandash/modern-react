class Hello extends React.Component {
    render(){
        return (
            <div>
                <h1>Hello World!</h1>
                <p>React App Layout Demo</p>
            </div>
        ) // JSX must return only one element
    }
}
